var nombre = "jhon",
  edad = 24;
function imprimirEdad() {
  console.log(`${nombre} tiene ${edad} años`);
}
function imprimirEdad(nombre, edad) {
  console.log(`${nombre} tiene ${edad} años`);
}
imprimirEdad();
